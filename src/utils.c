#include "utils.h"
#include <stdio.h>
#include <string.h>

void mysh_parse_command(const char* command,
                        int *argc, char*** argv)
{

//memory allocation
char*buf = NULL;
buf =( char*) malloc(1000);

char *p;




}
